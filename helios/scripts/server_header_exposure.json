{
  "name": "Server header exposure",
  "run_at": "response",
  "find": "once",
  "severity": 1,
  "options": ["passive"],
  "request": null,
  "matches": [
    {
      "type": "exists",
      "location": "headers",
      "match": "server",
      "options": [
        "ignore_case"
      ]
    }
  ]
}